import 'package:flutter/material.dart';

class KuryeGuncelSiparis extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      appBar: AppBar(
        title: Text('Adres:'),
      ),
      body: Center(
      ),
    );
  }
}

